# SQL Parser

Interpretação de *queries* SLQ com uma estrutura muito limitada.

```SQL
SELECT
    column1,
    column2,
    ...
FROM table 
    WHERE column_value =,<,>,<=,>= integer_value 
```
## Run

```
chmod +x run.sh
./run.sh
```